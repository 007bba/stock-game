# Stock Game 项目进度表

- 更新日期：2026-03-28
- 范围：MVP 基础层（DDL / API / ETL / Engine）
- 当前分支：`main`

## 总览

| 模块 | 状态 | 完成度 | 说明 |
|---|---|---:|---|
| 数据库 DDL (`db/schema.sql`) | 已完成 | 95% | 核心交易、行情、账本、ETL 作业表和索引已落地 |
| API 契约 (`docs/api/openapi.yaml`) | 已完成 | 90% | REST + WebSocket 事件模型已定义 |
| ETL 管道与压缩 (`scripts/etl`) | 已完成（本阶段） | 93% | 支持 all/validate、降级、报告落盘 |
| 规则与撮合引擎 (`scripts/engine`) | 已完成（P4） | 82% | 规则校验、批量撮合、账本更新、事务回滚 |
| 回放测试 (`tests/engine/test_replay_e2e.py`) | 已完成（P4） | 85% | 覆盖正常成交与主要拒单场景 |
| P5 交易服务集成（Engine + Service + API + Scheduler） | 已完成 | 100% | PgState 事务持久化、拒单码对齐、24/24 测试通过 |
| CI (`.github/workflows/etl-tests.yml`) | 已完成 | 95% | push/PR 单测 + workflow_dispatch/schedule Supabase DB 集成测试 |

## 已完成阶段

- P4 已完成：
  - 服务端权威规则引擎与撮合内核
  - 端到端回放测试（下单 -> 撮合 -> 账本）

## P5 本轮完成（2026-03-28）

- ✅ P5 交易服务集成完成度：**100%（已收口）**
- ✅ PgState 事务持久化（scripts/engine/pg_state.py）
- ✅ 关键 Bug 修复（跨赛季 ID 冲突、字段缺失、psycopg2 API）
- ✅ API 契约补齐：订单响应补充 `createdAt/updatedAt`，新增 cancel 路由断言
- ✅ 拒单码核对：`tests.service.test_api_orders` 覆盖 OpenAPI RejectCode 集合
- ✅ DB 集成测试命令可执行（Supabase 真实 DB 已接通，集成用例全部通过）
- ✅ 全量测试 24 项通过（0 skipped，DATABASE_URL 需传入 shell）
- ✅ PowerShell `.env` 自动加载脚本（`scripts/load_env.ps1`）与 ETL 启动脚本接入
- ✅ SeasonScheduler 依赖的 `market_ticks` 填充脚本（`scripts/service/fill_market_ticks.py`）
- ✅ CI workflow 补强：`workflow_dispatch` 支持 `RUN_DB_INTEGRATION=1` 真实 DB 测试
- 详细日志：`docs/plans/2026-03-28-p5-session-log.md`

## 下一阶段任务（P6 收尾与交付准备）

| 优先级 | 任务 | 目标 | 状态 |
|---:|---|---|
| P6 | PowerShell/CI 环境变量规范 | DATABASE_URL 从 .env/Secrets 注入 | 已完成（load_env + workflow secrets） |
| P6 | Scheduler 数据准备流程 | 一键补齐 season 对应 market_ticks | 已完成（新增 fill_market_ticks 脚本） |
| P6 | CI 触发策略确认 | push 跑单测，workflow_dispatch 跑 DB 集成 | 已完成（workflow 已更新） |

## P5 一键执行结果（2026-03-28）

> 补充：上次报告 "skipped: psycopg2 is not installed" 是因为 `DATABASE_URL` 未传入 shell 环境（`.env` 文件存在但 PowerShell 默认不自动加载）。传入后全部通过。

| Task | 命令 | 结果 |
|---|---|---|
| 1. Supabase/DB 连接验证 | `RUN_DB_INTEGRATION=1 python -m unittest tests.engine.test_pg_state_transaction -v` | `OK (2 tests)` |
| 1. Supabase/DB 连接验证 | `RUN_DB_INTEGRATION=1 python -m unittest tests.integration.test_trade_replay_db_flow -v` | `OK (1 test)` |
| 2. SeasonScheduler 验证 | `python -m unittest tests.service.test_season_scheduler -v` | `OK (1 test)` |
| 3. API 契约验证 | `python -m unittest tests.service.test_api_orders -v` | `OK (5 tests)` |
| 4. 事件流 sequence 验证 | `python -m unittest tests.service.test_events -v` | `OK (2 tests)` |
| 收口（真实 DB） | `RUN_DB_INTEGRATION=1 python -m unittest discover -s tests -p "test_*.py" -v` | `OK (24 tests, 0 skipped)` |

| P6 | GitHub Actions 云端验证 | 在仓库 Actions 中确认 push/workflow_dispatch 实际执行成功 | ✅ 已完成 |
| P6 | 交付文档打包 | 输出 runbook（本地/CI 测试、DB fixture、回放验证） | ✅ 已完成 |
| P6 | 发布前检查 | 完成 schema/API/测试报告一致性复核 | ✅ 已完成 |

## P6 收口完成（2026-03-29）

- ✅ CI 修复：改用本地 Postgres 容器替代 Supabase（解决 IPv6 连接问题）
- ✅ GitHub Actions 云端验证：push 触发 unit-tests 通过
- ✅ workflow_dispatch 触发 integration-fixture 通过
- ✅ 文档复核：openapi.yaml 补齐 userId/accountId
- ✅ 本地全量测试 24/24 通过
- ✅ 种子数据初始化验证通过
- ✅ market_ticks 填充验证通过（1200 条记录）

## 下一阶段任务（P7-P11）

详见：`docs/plans/2026-03-29-next-phase.md`

| Phase | 任务 | 优先级 |
|-------|------|--------|
| P7 | 前端界面开发 | P0 |
| P8 | 用户认证系统 | P0 |
| P9 | WebSocket 实时推送 | P1 |
| P10 | 生产环境部署 | P1 |
| P11 | 赛季管理与运营后台 | P2 |

## P5 本轮新增

- 引擎事务加固：
  - `tests/engine/test_pg_state_transaction.py`
  - `scripts/engine/pg_state.py`
  - `scripts/engine/orchestrator.py`
- 服务层与 API：
  - `scripts/service/trading_service.py`
  - `scripts/service/api.py`
  - `tests/service/test_trading_service.py`
  - `tests/service/test_api_orders.py`
- 调度与事件流：
  - `scripts/service/season_scheduler.py`
  - `scripts/service/events.py`
  - `tests/service/test_season_scheduler.py`
  - `tests/service/test_events.py`
  - `docs/reports/trade-replay-sample.json`
- DB 回放闭环：
  - `tests/integration/test_trade_replay_db_flow.py`
  - `tests/fixtures/minimal_season_fixture.json`
  - `scripts/etl/README.md`
- 收尾与交付：
  - `scripts/load_env.ps1`
  - `scripts/service/fill_market_ticks.py`
  - `.github/workflows/etl-tests.yml`

## 执行文档

- P5 实施计划：`docs/plans/2026-03-28-p5-trading-service-integration-implementation-plan.md`
- P5 AI 交接说明：`docs/plans/2026-03-28-ai-handoff-p5.md`
- 历史交接说明（P4）：`docs/plans/2026-03-28-ai-handoff-manual.md`

## 主要风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 分钟级行情权限受限 | 数据拉取存在限流与不可用 | 已有 `daily -> pseudo minute` 兜底 |
| fixture 测试依赖真实 DB | 环境差异导致波动 | `RUN_DB_INTEGRATION` 开关控制 |
| API 层尚未接通引擎 | 暂不可对外提供交易服务 | 按 P5 Task 1-6 分步接入 |
